// src/lib/timeline/constants.ts

export const TRACK_LABEL_WIDTH = 128; // Largura do painel de labels das tracks (w-32 em Tailwind)
export const DEFAULT_CLIP_DURATION_MS = 2000; // Duração padrão de um clipe em ms (2 segundos)
export const MIN_TIMELINE_DURATION_MS = 1000; // Duração mínima da timeline em ms (1 segundo)
export const MIN_CLIP_DISPLAY_WIDTH_PX = 10; // Largura mínima em pixels para exibir um clipe
export const ZOOM_LEVEL_MIN = 30; // Nível de zoom mínimo (px/s)
export const ZOOM_LEVEL_MAX = 400; // Nível de zoom máximo (px/s)
export const PLAYHEAD_WIDTH_PX = 12; // Largura do playhead em pixels